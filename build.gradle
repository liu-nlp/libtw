apply plugin: 'java'
apply plugin: 'maven-publish'

publishing {
	publications {
		mavenJava(MavenPublication) {
			groupId 'com.treewidth'
			artifactId 'libtw'
			version '1.0'
			from components.java
		}
	}
}
